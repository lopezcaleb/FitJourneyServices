export interface RoutineType {
    id: string,
    userId: string,
    name: string,
    exercises: string[],
    dateCreate: string
}

export interface ResRoutineType {
    id: string,
    name: string,
    exercises: string[],
    dateCreate: string
}